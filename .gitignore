# Treesitter parsers (compiled binaries, regenerated by :TSUpdate)
parser/

# Lazy plugin installs (stored in stdpath("data"), not here)
# lazy-lock.json is intentionally kept — it pins plugin versions

# Local machine overrides (not committed)
lua/local.lua
lua/local/

# Backup files
*.bak
*.orig

# Swap/undo files
*.swp
*.swo

# netrw history
.netrwhist

# macOS
.DS_Store
